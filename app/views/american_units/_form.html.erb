<%= form_for(@american_unit) do |f| %>
  <% if @american_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@american_unit.errors.count, "error") %> prohibited this american_unit from being saved:</h2>

      <ul>
      <% @american_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :designation %><br>
    <%= f.text_field :designation %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
