<%= form_for(@country) do |f| %>
  <% if @country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@country.errors.count, "error") %> prohibited this country from being saved:</h2>

      <ul>
      <% @country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :total_strength %><br>
    <%= f.number_field :total_strength %>
  </div>
  <div class="field">
    <%= f.label :aircraft %><br>
    <%= f.number_field :aircraft %>
  </div>
  <div class="field">
    <%= f.label :armored_vehicles %><br>
    <%= f.number_field :armored_vehicles %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
